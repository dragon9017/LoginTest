spring:
  thymeleaf:
    prefix: classpath:/templates/
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password
    url: jdbc:mysql://127.0.0.1:3306/login_test?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=GMT

server:
  port: 8848
mybatis:
  mapper-locations: classpath:Mapper/*.xml
  type-aliases-package: com.logintest.logintest.modal.entiry.*
#    实体类所在的包